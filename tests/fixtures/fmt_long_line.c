#include <stdint.h>

int32_t sum_five(int32_t alpha_value, int32_t beta_value, int32_t gamma_value, int32_t delta_value, int32_t epsilon_value)
{
    return alpha_value + beta_value + gamma_value + delta_value + epsilon_value;
}
